from database import Base
from sqlalchemy import Column, Integer, String

"""These models are the classes that will be persisted in a databse through an ORM, the schemas.py file contains the pydantic models (or schemas)."""